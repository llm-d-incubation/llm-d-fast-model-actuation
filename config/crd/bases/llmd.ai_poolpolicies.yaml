---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: poolpolicies.llmd.ai
spec:
  group: llmd.ai
  names:
    kind: ServerProviderProvisionPoolPolicy
    listKind: ServerProviderProvisionPoolPolicyList
    plural: poolpolicies
    singular: serverproviderprovisionpoolpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PoolPolicySpec defines the node-level idle pool configuration.
            properties:
              poolsPerNode:
                description: PoolsPerNode defines idle launcher counts per node type.
                items:
                  description: NodePoolSpec defines launcher count for a class of
                    nodes.
                  properties:
                    selector:
                      description: Selector describes the hardware characteristics
                        of target nodes.
                      properties:
                        acceleratorCount:
                          description: AcceleratorCount is the number of accelerators
                            required.
                          format: int32
                          type: integer
                        acceleratorType:
                          description: AcceleratorType is the GPU type (e.g., "nvidia.com/a100").
                          type: string
                        minMemoryGB:
                          description: MinMemoryGB is the minimum GPU memory in GB.
                          format: int32
                          type: integer
                      required:
                      - acceleratorCount
                      - acceleratorType
                      - minMemoryGB
                      type: object
                    totalCountPerTemplate:
                      description: |-
                        TotalCountPerTemplate is the total number of launcher or vLLM pods for each ServerProviderConfig
                        to maintain on each matching node.
                      items:
                        properties:
                          templateRef:
                            description: TemplateRef references the name of the ServerProviderConfig
                              this policy applies to.
                            properties:
                              name:
                                description: Name of the referenced ModelServerPool
                                type: string
                            required:
                            - name
                            type: object
                          totalCount:
                            description: TotalCount is the total number of idle launcher
                              pods to maintain.
                            format: int32
                            type: integer
                        required:
                        - totalCount
                        type: object
                      type: array
                  required:
                  - selector
                  - totalCountPerTemplate
                  type: object
                type: array
            required:
            - poolsPerNode
            type: object
          status:
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
