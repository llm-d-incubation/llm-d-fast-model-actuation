apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/name: launcher-populator
    app.kubernetes.io/component: controller
  name: launcher-population-policy-controller-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: launcher-population-policy-controller-role
subjects:
  - kind: ServiceAccount
    name: launcher-population-policy-controller
    namespace: {{ .Values.namespace }}
{{if .Values.NodeViewClusterRole}}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/name: launcher-populator
    app.kubernetes.io/component: controller
  name: launcher-population-policy-controller-node-view
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: "{{ .Values.NodeViewClusterRole }}"
subjects:
  - kind: ServiceAccount
    namespace: "{{ .Release.Namespace }}"
    name: launcher-population-policy-controller
{{end}}